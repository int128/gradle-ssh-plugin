allprojects {
    description = 'Groovy SSH library'
    group = 'org.hidetake'
    version = 'SNAPSHOT'

    // Set product version if tag is given on Travis CI
    System.getenv('TRAVIS_TAG')?.eachMatch(/^v([\d.]+)$/) { m -> version = m[1] }

    repositories {
        jcenter()
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.6'
}
